# Default: normalise line endings to LF on commit
* text=auto eol=lf

# Shell scripts must always be LF (runs on Jules' Ubuntu VM)
*.sh text eol=lf

# Windows-safe: JSON and Markdown can stay LF too
*.json text eol=lf
*.md   text eol=lf
*.py   text eol=lf
*.txt  text eol=lf
*.yml  text eol=lf
